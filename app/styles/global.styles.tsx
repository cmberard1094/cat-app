import { StyleSheet } from "react-native";

export const PRIMARY_COLOR = "#1e2749";

export const globalStyles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    flexDirection: "column",
    justifyContent: "center",
  },
});
